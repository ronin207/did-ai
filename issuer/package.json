{
  "name": "@did-ai/issuer",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "node --env-file=.env --watch dist/server.js",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && vitest run --reporter=dot"
  },
  "dependencies": {
    "@did-ai/shared": "file:../shared",
    "@fastify/cors": "^10.1.0",
    "fastify": "^5.0.0",
    "jose": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.9"
  }
}
